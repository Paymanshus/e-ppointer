/*import 'package:flutter/material.dart';

class NavBar extends StatefulWidget {
  @override
  _NavBarState createState() => _NavBarState();
}

class _NavBarState extends State<NavBar> {
  final shadowColor = const Color(0xFF141313);
  int selectedIndex = 0;
  /*final List<bool> index;
  final List<IconData> navIcon = [
    Icons.library_books,
    Icons.search,
    Icons.person
  ];
  -----------------endcommentshere------------------
  final bool active1, active2, active3;

  _NavBarState(this.active1, this.active2, this.active3);

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: <Widget>[
        Positioned(
          bottom: 45,
          width: MediaQuery.of(context).size.width,
          child: Row(
            mainAxisAlignment: MainAxisAlignment.spaceEvenly,
            children: <Widget>[
              _buildNavItem(Icons.library_books, active1),
              _buildNavItem(Icons.search, active2),
              _buildNavItem(Icons.person, active3),
            ],
          ),
        )
      ],
    );
  }
}

_buildNavItem(IconData icon, bool active) {
  return Container(
    decoration: BoxDecoration(
      color: Colors.white,
      shape: BoxShape.rectangle,
      boxShadow: [
        BoxShadow(
            color: const Color(0xFF141313),
            offset: Offset(0.0, 3.0),
            blurRadius: 6.0)
      ],
      borderRadius: BorderRadius.all(Radius.circular(13)),
    ),
    child: Icon(
      icon,
      color: active ? const Color(0xFFBF3528) : const Color(0xFFF4877D),
    ),
  );
}
*/
*/
